# --- Core ML ---
torch
torchvision
numpy
Pillow

# --- Model libraries ---
open_clip_torch
transformers
huggingface-hub

# --- Progress + utilities ---
tqdm
requests

# --- Project utilities (parsing, images, json etc.) ---
pyyaml

# sentencepiece  # optional, only needed for some HF tokenizers
# protobuf       # usually already provided with PyTorch / HF
